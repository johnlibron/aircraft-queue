<%= form_with(model: aircraft) do |form| %>
  <% if aircraft.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(aircraft.errors.count, "error") %> prohibited this aircraft from being saved:</h2>
      <ul>
        <% aircraft.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <br />

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <br />

  <div class="field">
    <%= form.label :type %>
    <%= form.select :type, ["Passenger", "Cargo"] %>
  </div>

  <br />

  <div class="field">
    <%= form.label :size %>
    <%= form.select :size, ["Small", "Large"] %>
  </div>

  <br />

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
